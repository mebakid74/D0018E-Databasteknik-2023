const isValidId = (v) => {
    return true;
}

const isValidEmail = (v) => {
    return true;
}

const isValidPassword = (v) => {
    return true;
}

module.exports = {isValidEmail, isValidId, isValidPassword}